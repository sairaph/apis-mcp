---
title: Lists indicators across multiple datasets
page_id: operation-get-accounts-account-id-cloudforce-one-events-indicators-447606d4
path: operations/indicator
description: Retrieves a paginated list of indicators across specified datasets. Use datasetIds=all or datasetIds=* to query all datasets for the account. If no datasetIds provided, uses the default dataset.
source: https://raw.githubusercontent.com/cloudflare/api-schemas/c92b9b0fde23ae00fece2025662f96dc8e2d6283/openapi.json
http_methods:
    - GET
api_endpoints:
    - /accounts/{account_id}/cloudforce-one/events/indicators
operation_ids:
    - get_IndicatorList
source_type: openapi
imported_from: https://raw.githubusercontent.com/cloudflare/api-schemas/c92b9b0fde23ae00fece2025662f96dc8e2d6283/openapi.json
---

# Lists indicators across multiple datasets

`GET /accounts/{account_id}/cloudforce-one/events/indicators`

Operation ID: `get_IndicatorList`

Retrieves a paginated list of indicators across specified datasets. Use datasetIds=all or datasetIds=* to query all datasets for the account. If no datasetIds provided, uses the default dataset.

## Definition

```yaml
{"operationId": "get_IndicatorList", "summary": "Lists indicators across multiple datasets", "description": "Retrieves a paginated list of indicators across specified datasets. Use datasetIds=all or datasetIds=* to query all datasets for the account. If no datasetIds provided, uses the default dataset.", "parameters": [{"name": "account_id", "in": "path", "description": "Account ID.", "required": true, "schema": {"description": "Account ID.", "type": "string"}}, {"name": "datasetIds", "in": "query", "description": "Dataset IDs to query indicators from (array of UUIDs), or special value 'all' or '*' to query all datasets. If not provided, uses the default dataset.", "schema": {"description": "Dataset IDs to query indicators from (array of UUIDs), or special value 'all' or '*' to query all datasets. If not provided, uses the default dataset.", "type": "array", "items": {"type": "string"}}}, {"name": "page", "in": "query", "schema": {"type": "number"}}, {"name": "pageSize", "in": "query", "schema": {"type": "number"}}, {"name": "search", "in": "query", "description": "Structured search as a JSON array of {field, op, value} objects. Searchable fields: value, indicatorType, uuid. Supports operators: equals, not, contains, startsWith, endsWith, gt, lt, gte, lte, like, in, find. Use the 'in' operator with an array value to bulk-check up to 100 indicators in a single request, e.g. search=[{\"field\":\"value\",\"op\":\"in\",\"value\":[\"evil.com\",\"bad.org\"]}]. Multiple conditions are AND'd together. Max 10 conditions per request.", "schema": {"description": "Structured search as a JSON array of {field, op, value} objects. Searchable fields: value, indicatorType, uuid. Supports operators: equals, not, contains, startsWith, endsWith, gt, lt, gte, lte, like, in, find. Use the 'in' operator with an array value to bulk-check up to 100 indicators in a single request, e.g. search=[{\"field\":\"value\",\"op\":\"in\",\"value\":[\"evil.com\",\"bad.org\"]}]. Multiple conditions are AND'd together. Max 10 conditions per request.", "type": "array", "items": {"properties": {"field": {"description": "The indicator field to search on. Allowed: value, indicatorType, uuid.", "type": "string", "example": "value", "enum": ["value", "indicatorType", "uuid"]}, "op": {"description": "Search operator. Use 'in' for bulk lookup of up to 100 values at once, e.g. {field:'value', op:'in', value:['evil.com','bad.org']}.", "type": "string", "example": "contains", "enum": ["equals", "not", "gt", "gte", "lt", "lte", "like", "contains", "startsWith", "endsWith", "in", "find"]}, "value": {"description": "Search value. String for most operators. Array of strings for 'in' operator (max 100 items).", "anyOf": [{"example": "malicious", "type": "string"}, {"items": {"type": "string"}, "type": "array"}]}}, "required": ["field", "op", "value"], "type": "object"}}}, {"name": "name", "in": "query", "description": "Filter indicators by value using substring match (LIKE). Legacy alternative to structured search.", "schema": {"description": "Filter indicators by value using substring match (LIKE). Legacy alternative to structured search.", "type": "string"}}, {"name": "indicatorType", "in": "query", "schema": {"type": "string"}}, {"name": "relatedEvents", "in": "query", "description": "Filter by related event IDs", "schema": {"description": "Filter by related event IDs", "type": "array", "items": {"type": "string"}}}, {"name": "tags", "in": "query", "description": "Filter by tag values or UUIDs. Indicators must have at least one of the specified tags (OR logic). Supports both tag UUID and tag value.", "schema": {"description": "Filter by tag values or UUIDs. Indicators must have at least one of the specified tags (OR logic). Supports both tag UUID and tag value.", "type": "array", "items": {"type": "string"}}}, {"name": "tagSearch", "in": "query", "description": "Structured tag-metadata filter as a JSON array of {field, op, value} objects. Operates against the per-dataset IndicatorTag mirror so you can find indicators by tag attributes (origin country, motive, sophistication, priority, etc.) without a separate Tags lookup. Common dashboard usage: drill from a country into indicators, e.g. tagSearch=[{\"field\":\"originCountryISO\",\"op\":\"in\",\"value\":[\"IR\",\"CN\"]}]. Country values may be passed as alpha-2, alpha-3, name, or alias (e.g. \"iran\"). Operators: equals, not, gt/gte/lt/lte (numeric only), contains/like/find/startsWith/endsWith (string only), in. AND-joined across entries; combined with `tags`, a matching tag must satisfy both. Max 10 entries per request, max 100 values per 'in'. Performance notes: `originCountryISO` uses its B-tree index for equals/not/in. `priority` uses its B-tree index for numeric comparisons. Other string columns (`actorCategory`, `motive`, etc.) are case-insensitive and unindexed; current catalog size makes this a non-issue. `endsWith` and `aliasGroupNames` contains/like are leading-wildcard scans and slow on large result sets. `aliasGroupNames` matches on the JSON-encoded text, so substrings can cross alias boundaries (\"apt28\" also matches \"apt280\" when both appear in the same tag's alias list).", "schema": {"description": "Structured tag-metadata filter as a JSON array of {field, op, value} objects. Operates against the per-dataset IndicatorTag mirror so you can find indicators by tag attributes (origin country, motive, sophistication, priority, etc.) without a separate Tags lookup. Common dashboard usage: drill from a country into indicators, e.g. tagSearch=[{\"field\":\"originCountryISO\",\"op\":\"in\",\"value\":[\"IR\",\"CN\"]}]. Country values may be passed as alpha-2, alpha-3, name, or alias (e.g. \"iran\"). Operators: equals, not, gt/gte/lt/lte (numeric only), contains/like/find/startsWith/endsWith (string only), in. AND-joined across entries; combined with `tags`, a matching tag must satisfy both. Max 10 entries per request, max 100 values per 'in'. Performance notes: `originCountryISO` uses its B-tree index for equals/not/in. `priority` uses its B-tree index for numeric comparisons. Other string columns (`actorCategory`, `motive`, etc.) are case-insensitive and unindexed; current catalog size makes this a non-issue. `endsWith` and `aliasGroupNames` contains/like are leading-wildcard scans and slow on large result sets. `aliasGroupNames` matches on the JSON-encoded text, so substrings can cross alias boundaries (\"apt28\" also matches \"apt280\" when both appear in the same tag's alias list).", "type": "array", "items": {"properties": {"field": {"description": "Tag mirror field to filter on. Allowed: value, categoryId, actorCategory, aliasGroupNames, attributionConfidence, attributionOrganization, motive, opsecLevel, originCountryISO, sophisticationLevel, priority, analyticPriority. Filters operate against the per-dataset IndicatorTag mirror (which is kept in sync with the Tags SoT by the tag-propagation workflow).", "type": "string", "example": "originCountryISO", "enum": ["value", "categoryId", "actorCategory", "aliasGroupNames", "attributionConfidence", "attributionOrganization", "motive", "opsecLevel", "originCountryISO", "sophisticationLevel", "priority", "analyticPriority"]}, "op": {"description": "Search operator. Use 'in' for bulk OR within a single field, e.g. {field:\"originCountryISO\", op:\"in\", value:[\"IR\",\"CN\"]}.", "type": "string", "example": "in", "enum": ["equals", "not", "gt", "gte", "lt", "lte", "like", "contains", "startsWith", "endsWith", "in", "find"]}, "value": {"description": "Search value. String or number for most operators. Array for 'in' (max 100 items). Country values may be passed as alpha-2, alpha-3, name, or common alias (e.g. \"iran\", \"IR\", \"IRN\") and are normalized to alpha-2 server-side.", "anyOf": [{"example": "IR", "type": "string"}, {"example": 5, "type": "number"}, {"items": {"anyOf": [{"type": "string"}, {"type": "number"}]}, "type": "array"}]}}, "required": ["field", "op"], "type": "object"}}}, {"name": "createdAfter", "in": "query", "description": "Filter indicators created on or after this date. Must use ISO 8601 format (e.g., '2024-01-15T00:00:00Z').", "schema": {"description": "Filter indicators created on or after this date. Must use ISO 8601 format (e.g., '2024-01-15T00:00:00Z').", "type": "string", "format": "date-time"}}, {"name": "createdBefore", "in": "query", "description": "Filter indicators created on or before this date. Must use ISO 8601 format (e.g., '2024-12-31T23:59:59Z').", "schema": {"description": "Filter indicators created on or before this date. Must use ISO 8601 format (e.g., '2024-12-31T23:59:59Z').", "type": "string", "format": "date-time"}}, {"name": "relatedEventsLimit", "in": "query", "description": "Limit the number of related events returned per indicator. Default: 2. Set to 0 for none, -1 for all events.", "schema": {"description": "Limit the number of related events returned per indicator. Default: 2. Set to 0 for none, -1 for all events.", "type": "number", "example": 2}}, {"name": "includeTags", "in": "query", "description": "Whether to include full tag details for each indicator. Defaults to true.", "schema": {"description": "Whether to include full tag details for each indicator. Defaults to true.", "type": "boolean"}}, {"name": "includeTotalCount", "in": "query", "description": "Whether to compute accurate total count via COUNT(*). Defaults to false for performance. When false, total_count is an approximation.", "schema": {"description": "Whether to compute accurate total count via COUNT(*). Defaults to false for performance. When false, total_count is an approximation.", "type": "boolean"}}, {"name": "format", "in": "query", "description": "Output format for indicator data. 'json' returns the default format, 'stix2' returns STIX 2.1 Indicator SDOs, 'taxii' returns a TAXII 2.1 Envelope with Content-Type application/taxii+json;version=2.1.", "schema": {"description": "Output format for indicator data. 'json' returns the default format, 'stix2' returns STIX 2.1 Indicator SDOs, 'taxii' returns a TAXII 2.1 Envelope with Content-Type application/taxii+json;version=2.1.", "type": "string", "example": "json", "enum": ["json", "stix2", "taxii"]}}, {"name": "source", "in": "query", "description": "Read backend. 'do' (default) reads Durable Object storage. 'r2catalog' reads R2 Data Catalog (admin-only, experimental; supports a subset of search fields).", "schema": {"description": "Read backend. 'do' (default) reads Durable Object storage. 'r2catalog' reads R2 Data Catalog (admin-only, experimental; supports a subset of search fields).", "type": "string", "enum": ["do", "r2catalog"]}}, {"name": "cache", "in": "query", "description": "Cache strategy. 'from-graph' serves results from the graph-node KV cache when all requested UUIDs are cached; falls back to normal path on partial/zero hit.", "schema": {"description": "Cache strategy. 'from-graph' serves results from the graph-node KV cache when all requested UUIDs are cached; falls back to normal path on partial/zero hit.", "type": "string", "enum": ["from-graph"]}}], "responses": {"200": {"description": "Returns a paginated list of indicators.", "content": {"application/json": {"schema": {"type": "object", "properties": {"properties": {"type": "object", "properties": {"indicators": {"properties": {"items": {"type": "object", "properties": {"createdAt": {"type": "string", "format": "date-time", "example": "2022-04-01T00:00:00Z"}, "datasetId": {"description": "The dataset ID this indicator belongs to. Included in list responses.", "type": "string", "example": "dataset-uuid-123"}, "indicatorType": {"type": "string", "example": "domain"}, "relatedEvents": {"type": "array", "items": {"properties": {"datasetId": {"type": "string", "example": "dataset-uuid-123"}, "eventId": {"type": "string", "example": "event-uuid-456"}}, "required": ["datasetId", "eventId"], "type": "object"}}, "tags": {"type": "array", "items": {"properties": {"categoryName": {"type": "string"}, "uuid": {"type": "string"}, "value": {"type": "string"}}, "type": "object"}}, "updatedAt": {"type": "string", "format": "date-time", "example": "2022-04-01T00:00:00Z"}, "uuid": {"type": "string", "example": "12345678-1234-1234-1234-1234567890ab"}, "value": {"type": "string", "example": "malicious-domain.com"}}, "required": ["uuid", "indicatorType", "value", "createdAt", "updatedAt"]}, "type": {"type": "string", "example": "array"}}, "required": ["type", "items"], "type": "object"}, "pagination": {"properties": {"properties": {"type": "object", "properties": {"count": {"properties": {"type": {"type": "string", "example": "number"}}, "required": ["type"], "type": "object"}, "page": {"properties": {"type": {"type": "string", "example": "number"}}, "required": ["type"], "type": "object"}, "per_page": {"properties": {"type": {"type": "string", "example": "number"}}, "required": ["type"], "type": "object"}, "total_count": {"properties": {"type": {"type": "string", "example": "number"}}, "required": ["type"], "type": "object"}}, "required": ["count", "page", "per_page", "total_count"]}, "type": {"type": "string", "example": "object"}}, "required": ["type", "properties"], "type": "object"}}, "required": ["indicators", "pagination"]}, "type": {"type": "string", "example": "object"}}, "required": ["type", "properties"]}}}}, "400": {"description": "Bad Request.", "content": {"application/json": {"schema": {"type": "object", "properties": {"errors": {"type": "array", "items": {"properties": {"message": {"type": "string", "example": "An error occurred."}}, "required": ["message"], "type": "object"}}, "result": {"type": "object"}, "success": {"type": "boolean"}}, "required": ["result", "success", "errors"]}}}}}, "security": [{"api_token": []}], "tags": ["Indicator"], "x-api-token-group": ["Cloudforce One Write", "Cloudforce One Read"]}
```

---
title: Lists all tags (SoT)
page_id: operation-get-accounts-account-id-cloudforce-one-events-tags-29dba82a
path: operations/tag
description: Returns all Source-of-Truth tags for an account. Supports legacy free-text `search` on tag value and `categoryUuid` exact match, plus a structured `filters` JSON array for filtering by metadata fields (originCountryISO, actorCategory, motive, priority, etc.). Country values may be passed as alpha-2, alpha-3, name, or common alias.
source: https://raw.githubusercontent.com/cloudflare/api-schemas/c92b9b0fde23ae00fece2025662f96dc8e2d6283/openapi.json
http_methods:
    - GET
api_endpoints:
    - /accounts/{account_id}/cloudforce-one/events/tags
operation_ids:
    - get_TagList
source_type: openapi
imported_from: https://raw.githubusercontent.com/cloudflare/api-schemas/c92b9b0fde23ae00fece2025662f96dc8e2d6283/openapi.json
---

# Lists all tags (SoT)

`GET /accounts/{account_id}/cloudforce-one/events/tags`

Operation ID: `get_TagList`

Returns all Source-of-Truth tags for an account. Supports legacy free-text `search` on tag value and `categoryUuid` exact match, plus a structured `filters` JSON array for filtering by metadata fields (originCountryISO, actorCategory, motive, priority, etc.). Country values may be passed as alpha-2, alpha-3, name, or common alias.

## Definition

```yaml
{"operationId": "get_TagList", "summary": "Lists all tags (SoT)", "description": "Returns all Source-of-Truth tags for an account. Supports legacy free-text `search` on tag value and `categoryUuid` exact match, plus a structured `filters` JSON array for filtering by metadata fields (originCountryISO, actorCategory, motive, priority, etc.). Country values may be passed as alpha-2, alpha-3, name, or common alias.", "parameters": [{"name": "account_id", "in": "path", "description": "Account ID.", "required": true, "schema": {"description": "Account ID.", "type": "string"}}, {"name": "page", "in": "query", "schema": {"type": "number"}}, {"name": "pageSize", "in": "query", "schema": {"type": "number"}}, {"name": "search", "in": "query", "description": "Legacy free-text substring match on tag value.", "schema": {"description": "Legacy free-text substring match on tag value.", "type": "string"}}, {"name": "categoryUuid", "in": "query", "schema": {"type": "string"}}, {"name": "filters", "in": "query", "description": "Structured filters as a JSON array of {field, op, value} objects. Searchable fields: uuid, value, actorCategory, actorCategoryConfidence, aliasGroupNames, attributionConfidence, attributionConfidenceScore, attributionOrganization, categoryName, motive, motiveConfidence, opsecLevel, originCountryISO, originCountryConfidence, sophisticationLevel, priority, analyticPriority. Operators: equals, not, contains, startsWith, endsWith, gt, lt, gte, lte, like, in, find. Use 'in' for bulk OR within a single field, e.g. filters=[{\"field\":\"originCountryISO\",\"op\":\"in\",\"value\":[\"IR\",\"CN\"]}]. Multiple entries are AND-joined. Max 10 entries per request, max 100 values per 'in'. Per-field notes: `uuid` accepts only 'equals' and 'in' (other operators throw ValidationError) — matched against the canonical lowercase storage but callers may pass either case (the server lowercases before comparison); index-backed by the column's UNIQUE constraint and intended for batched UUID → tag resolution. `originCountryISO` uses its B-tree index for equals/not/in. `priority` uses its B-tree index for numeric comparisons. Other string columns (`actorCategory`, `motive`, etc.) are case-insensitive and unindexed; current catalog size makes this a non-issue. `endsWith` and `aliasGroupNames` contains/like are leading-wildcard scans and slow on large result sets. `aliasGroupNames` matches on the JSON-encoded text, so substrings can cross alias boundaries (a search for \"apt28\" will also match \"apt280\" if both appear in the same tag's alias list).", "schema": {"description": "Structured filters as a JSON array of {field, op, value} objects. Searchable fields: uuid, value, actorCategory, actorCategoryConfidence, aliasGroupNames, attributionConfidence, attributionConfidenceScore, attributionOrganization, categoryName, motive, motiveConfidence, opsecLevel, originCountryISO, originCountryConfidence, sophisticationLevel, priority, analyticPriority. Operators: equals, not, contains, startsWith, endsWith, gt, lt, gte, lte, like, in, find. Use 'in' for bulk OR within a single field, e.g. filters=[{\"field\":\"originCountryISO\",\"op\":\"in\",\"value\":[\"IR\",\"CN\"]}]. Multiple entries are AND-joined. Max 10 entries per request, max 100 values per 'in'. Per-field notes: `uuid` accepts only 'equals' and 'in' (other operators throw ValidationError) — matched against the canonical lowercase storage but callers may pass either case (the server lowercases before comparison); index-backed by the column's UNIQUE constraint and intended for batched UUID → tag resolution. `originCountryISO` uses its B-tree index for equals/not/in. `priority` uses its B-tree index for numeric comparisons. Other string columns (`actorCategory`, `motive`, etc.) are case-insensitive and unindexed; current catalog size makes this a non-issue. `endsWith` and `aliasGroupNames` contains/like are leading-wildcard scans and slow on large result sets. `aliasGroupNames` matches on the JSON-encoded text, so substrings can cross alias boundaries (a search for \"apt28\" will also match \"apt280\" if both appear in the same tag's alias list).", "type": "array", "items": {"properties": {"field": {"description": "Tag field to search on. Allowed: uuid, value, actorCategory, actorCategoryConfidence, aliasGroupNames, attributionConfidence, attributionConfidenceScore, attributionOrganization, categoryName, motive, motiveConfidence, opsecLevel, originCountryISO, originCountryConfidence, sophisticationLevel, priority, analyticPriority.", "type": "string", "example": "originCountryISO", "enum": ["uuid", "value", "actorCategory", "actorCategoryConfidence", "aliasGroupNames", "attributionConfidence", "attributionConfidenceScore", "attributionOrganization", "categoryName", "motive", "motiveConfidence", "opsecLevel", "originCountryISO", "originCountryConfidence", "sophisticationLevel", "priority", "analyticPriority"]}, "op": {"description": "Search operator. Use 'in' for bulk OR within a single field, e.g. {field:\"originCountryISO\", op:\"in\", value:[\"IR\",\"CN\"]}.", "type": "string", "example": "in", "enum": ["equals", "not", "gt", "gte", "lt", "lte", "like", "contains", "startsWith", "endsWith", "in", "find"]}, "value": {"description": "Search value. String or number for most operators. Array for 'in' (max 100 items). Country values may be passed as alpha-2, alpha-3, name, or common alias (e.g. 'iran', 'IR', 'IRN') and are normalized to alpha-2 server-side.", "anyOf": [{"example": "IR", "type": "string"}, {"example": 5, "type": "number"}, {"items": {"anyOf": [{"type": "string"}, {"type": "number"}]}, "type": "array"}]}}, "required": ["field", "op"], "type": "object"}}}, {"name": "cache", "in": "query", "description": "Cache strategy. 'from-graph' serves results from the graph-node KV cache when all requested UUIDs are cached; falls back to normal path on partial/zero hit.", "schema": {"description": "Cache strategy. 'from-graph' serves results from the graph-node KV cache when all requested UUIDs are cached; falls back to normal path on partial/zero hit.", "type": "string", "enum": ["from-graph"]}}], "responses": {"200": {"description": "Returns a paginated list of tags.", "content": {"application/json": {"schema": {"type": "object", "properties": {"pagination": {"type": "object", "properties": {"page": {"type": "number"}, "pageSize": {"type": "number"}, "totalCount": {"type": "number"}, "totalPages": {"type": "number"}}, "required": ["page", "pageSize", "totalCount", "totalPages"]}, "tags": {"type": "array", "items": {"properties": {"activeDuration": {"type": "string"}, "actorCategory": {"type": "string"}, "actorCategoryConfidence": {"description": "Confidence (1-10) in the actor variety (actorCategory). CFONE-only: stripped from responses to non-CFONE accounts.", "type": "integer", "example": 7, "maximum": 10, "minimum": 1}, "aliasGroupNames": {"type": "array", "items": {"type": "string"}}, "aliasGroupNamesInternal": {"type": "array", "items": {"type": "string"}}, "aliases": {"description": "Structured aliases ({ value, confidence 1-10, tlp }). CFONE-only: stripped from responses to non-CFONE accounts.", "type": "array", "items": {"properties": {"confidence": {"type": "integer", "example": 8, "maximum": 10, "minimum": 1, "nullable": true}, "tlp": {"type": "string", "example": "amber", "enum": ["red", "amber", "green", "white"], "nullable": true}, "value": {"type": "string", "example": "Fancy Bear"}}, "required": ["value"], "type": "object"}}, "analyticPriority": {"type": "number"}, "attributionConfidence": {"type": "string"}, "attributionConfidenceScore": {"type": "integer", "example": 7, "maximum": 10, "minimum": 1}, "attributionOrganization": {"type": "string"}, "categoryName": {"type": "string", "example": "Nation State"}, "categoryUuid": {"type": "string", "example": "12345678-1234-1234-1234-1234567890ab"}, "dateOfDiscovery": {"type": "string", "example": "2024-01-15"}, "externalReferenceLinks": {"type": "array", "items": {"type": "string"}}, "externalReferences": {"description": "Structured external references ({ url, description }). Public: returned to all accounts.", "type": "array", "items": {"properties": {"description": {"type": "string", "example": "Vendor threat report", "nullable": true}, "url": {"type": "string", "example": "https://example.com/report"}}, "required": ["url"], "type": "object"}}, "internalAliases": {"description": "Internal structured aliases ({ value, confidence 1-10, tlp }). CFONE-only: never returned to non-CFONE accounts.", "type": "array", "items": {"properties": {"confidence": {"type": "integer", "example": 8, "maximum": 10, "minimum": 1, "nullable": true}, "tlp": {"type": "string", "example": "amber", "enum": ["red", "amber", "green", "white"], "nullable": true}, "value": {"type": "string", "example": "Fancy Bear"}}, "required": ["value"], "type": "object"}}, "internalDescription": {"type": "string"}, "motive": {"type": "string"}, "motiveConfidence": {"description": "Confidence (1-10) in the actor motive. CFONE-only: stripped from responses to non-CFONE accounts.", "type": "integer", "example": 7, "maximum": 10, "minimum": 1}, "opsecLevel": {"type": "string"}, "originCountryConfidence": {"description": "Confidence (1-10) in the origin-country attribution. CFONE-only: stripped from responses to non-CFONE accounts.", "type": "integer", "example": 7, "maximum": 10, "minimum": 1}, "originCountryISO": {"type": "string"}, "originCountryISOAlpha3": {"type": "string", "example": "IRN"}, "originCountryTlp": {"description": "TLP marking for the origin-country attribution. CFONE-only: stripped from responses to non-CFONE accounts.", "type": "string", "example": "amber", "enum": ["red", "amber", "green", "white"]}, "priority": {"type": "number"}, "sophisticationLevel": {"type": "string"}, "uuid": {"type": "string", "example": "12345678-1234-1234-1234-1234567890ab"}, "value": {"type": "string", "example": "APT28"}}, "required": ["uuid", "value"], "type": "object"}}}, "required": ["tags", "pagination"]}}}}, "400": {"description": "Bad Request.", "content": {"application/json": {"schema": {"type": "object", "properties": {"errors": {"type": "array", "items": {"properties": {"message": {"type": "string", "example": "An error occurred."}}, "required": ["message"], "type": "object"}}, "result": {"type": "object"}, "success": {"type": "boolean"}}, "required": ["result", "success", "errors"]}}}}}, "security": [{"api_token": []}], "tags": ["Tag"], "x-api-token-group": ["Cloudforce One Write", "Cloudforce One Read"]}
```

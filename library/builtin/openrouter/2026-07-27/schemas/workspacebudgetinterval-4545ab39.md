---
title: WorkspaceBudgetInterval
page_id: schema-workspacebudgetinterval-4545ab39
path: schemas
description: Budget reset interval. Use "lifetime" for a one-time budget that never resets.
source: https://openrouter.ai/openapi.json
source_type: openapi
imported_from: https://openrouter.ai/openapi.json
---

# WorkspaceBudgetInterval

Budget reset interval. Use "lifetime" for a one-time budget that never resets.

```yaml
{"description": "Budget reset interval. Use \"lifetime\" for a one-time budget that never resets.", "enum": ["daily", "weekly", "monthly", "lifetime"], "example": "monthly", "type": "string", "x-speakeasy-unknown-values": "allow"}
```
